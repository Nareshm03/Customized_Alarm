<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ========================================== -->
    <!-- DARK MODE THEME (Auto-applied at night) -->
    <!-- ========================================== -->
    <!-- The AppTheme is defined in values/themes.xml as dark mode by default -->
    <!-- This file is for system dark mode compatibility -->

    <!-- Legacy theme names for backward compatibility -->
    <style name="Theme.TeacherScheduler" parent="AppTheme" />
    
    <style name="Theme.TeacherScheduler.NoActionBar" parent="AppTheme" />
</resources>